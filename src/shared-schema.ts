import { defineMessages } from "react-intl"

export const sharedErrors = defineMessages({
  required: {
    id: "sharedErrors.required",
    defaultMessage: "Required",
  },
  invalid: {
    id: "sharedErrors.invalid",
    defaultMessage: "Invalid",
  },
  errorTitle: {
    id: "sharedErrors.errorTitle",
    defaultMessage: "Oops!",
  },
})
